1단계: 기존 코드 정리 및 기반 다지기
[x] [삭제] 기존 언더컷 및 축소 정복 로직 제거기존 코드에서 축소 정복(이진 탐색)으로 구현되었던 언더컷 타이밍 서치 관련 함수 및 변수들을 깔끔하게 삭제.
[x] [공간으로 시간 벌기 - 신규] 서킷 및 드라이버 데이터 캐싱기존 코드에서 서킷 섹터 정보, 드라이버별 성향 가중치 데이터 로딩부 확인.메인 재생 루프 밖(초기화 단계)에서 이를 Dictionary 또는 2차원 룩업 테이블(Lookup Table) 배열에 박아두고, 루프 내에서는 $O(1)$로만 참조하도록 수정하여 백그라운드 연산 부하에 대비.


2단계: 실시간 재생 루프 고도화 (기존 코드 기능 연결)
[x] [분할 정복 - 기존] 최근접 차량 쌍(Closest Pair) 배틀 감지 점검매 랩마다 차량들의 GPS 좌표를 받아 가장 가까운 차량 쌍을 반환하는 기존 분할 정복 로직이 스트리밍 루프 내에서 정상 작동하는지 확인.두 차량의 격차가 기준치(예: 0.5초 이내) 이하일 때 화면에 [🔥 BATTLE TRIGGERED] 알림 배너가 실시간으로 찍히도록 연동.
[x] [억지 기법 - 기존] DRS 디텍션 존 전수 체크 연결배틀 중인 차량들이 서킷의 DRS 디텍션 포인트 좌표를 통과하는 순간, 두 차의 시간 간격을 전수 조사(if문 단순 비교)하는 기존 로직 검증.1초 이내일 경우 화면 차량 정보 옆에 [DRS AVAILABLE] 플래그가 실시간으로 켜지도록 시각화 연출 보완.

3단계: 백그라운드 예측 엔진 탑재 (신규 알고리즘 추가)
[x] [동적 계획법 - 신규] 실시간 예상 완주 시간(ETA) 예측기 구현현재 재생 중인 랩($N$)의 실제 타이어 마모도를 초기 상태로 잡고, 남은 랩 동안의 페이스 저하 추이를 누적 연산하는 DP 점화식 설계.중복 연산을 방지하기 위한 메모이제이션(Memoization) 테이블(배열) 적용.메인 루프에서 매 랩이 끝날 때마다 이 DP 함수를 호출해 대시보드 한쪽에 [AI 예상 완주 타임: XX:XX]이 실시간으로 변하며 갱신되게 구현.
[x] [분기 한정 - 신규] 돌발 상황 대안 피트스톱 최적화 엔진 구현2021 프랑스 GP 데이터 중 막스 베르스타펜이 피트인 하는 시점(혹은 특정 이벤트 랩)에 트리거되는 함수 작성.남은 랩 동안 선택 가능한 타이어 시퀀스(소프트/미디엄/하드) 상태 공간 트리 설계.탐색 도중 특정 경로의 예상 시간 하한선(Bound)이 이미 구해놓은 최적 시간보다 느리면 과감히 잘라내는 Pruning(가지치기) 로직 구현.실제 역사적 데이터(원스톱 vs 투스톱)와 분기 한정이 찾아낸 최적의 대안 전술 경로를 화면에 비교 팝업으로 출력.

4단계: 통합 테스트 및 디버깅
[x] 레이스 타임라인을 1랩부터 끝까지 스트리밍 재생하면서 새로 추가한 DP와 분기 한정 연산 때문에 화면 랙이 걸리지 않는지 확인 (공간으로 시간벌기 캐싱 성능 검증).
[x] 마지막 2랩에서 베르스타펜이 해밀턴을 DRS로 따돌리는 순간 분할 정복 배틀 배너와 DRS 불빛이 타이밍 맞게 연출되는지 최종 마일스톤 확인.
[x] 결과 보고서 제출을 위해 5가지 알고리즘이 각각 어느 클래스/함수에서 실행되는지 주석 정비 및 로그 수집.

5단계: 유저 피드백
[ ] 유저 피드백을 받아 ui 수정